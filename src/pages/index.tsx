import HomePage from "./home"

// Re-export the HomePage component as the default export for the index page
export default HomePage

// Re-export the Head component from the HomePage component
export { Head } from "./home" 